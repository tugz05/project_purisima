/**
 * JSON fetch with session cookie + CSRF for Laravel messaging routes.
 * Same implementation as laravelJsonFetch in @/utils/laravelJsonFetch.
 */
export { laravelJsonFetch as messagingJsonFetch } from '@/utils/laravelJsonFetch';
